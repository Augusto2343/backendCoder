<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #3d7a8b, #2b5763 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        .login-card {
            background: white;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            padding: 2rem;
            width: 100%;
            max-width: 400px;
        }
        
        .login-title {
            text-align: center;
            color: #333;
            margin-bottom: 2rem;
        }
        
        .form-control:focus {
            border-color: #667eea;
            box-shadow: 0 0 0 0.2rem rgba(102, 126, 234, 0.25);
        }
        
        .btn-login {
            background: linear-gradient(45deg, #66cdea, #2b5763);
            border: none;
            width: 100%;
            padding: 12px;
            border-radius: 8px;
            color: white;
            font-weight: 600;
            transition: transform 0.2s;
        }
        
        .btn-login:hover {
            transform: translateY(-2px);
            color: white;
        }
        .backgroundForm{
            background-color: #d3d3d3;
        }

    </style>
</head>
<body class="d-flex flex-column justify-center align-center g">
    <section class="backgroundForm p-4 rounded-3 d-flex flex-column justify-center align-items-center g w-50" >
    <div class="mb-10">
            <a  href="/login" class="btn btn-primary  disabled bg-dark ">
                    Loguearse
            </a >
                    
            <a href="/register" class="btn btn-primary ">
                    Registrarse
            </a>
        </div>
    <div class="login-card">
        {{#if error}}
        <div class="alert alert-danger" role="alert">{{error}}</div>
        {{/if}}
        {{#if name}}
        <div class="alert alert-success" role="alert">{{message}} {{name}}</div>
        
        {{/if}}
        <h2 class="login-title">Iniciar Sesión</h2>
        
        <form action="/api/login" method="post" id="loginForm">
            <div class="mb-3">
                <label for="email" class="form-label">Email</label>
                <input 
                    type="email" 
                    class="form-control" 
                    id="email" 
                    name="email" 
                    placeholder="tu@email.com"
                    required
                >
            </div>
            
            <div class="mb-3">
                <label for="password" class="form-label">Contraseña</label>
                <input 
                    type="password" 
                    class="form-control" 
                    id="password" 
                    name="password" 
                    placeholder="Tu contraseña"
                    required
                >
            </div>
            
            <button type="submit" id="submitButton" class="btn btn-login">
                Iniciar Sesión
            </button>
        </form>
    </div>
    </section>
<script src="/js/login.js"></script>
</body>
</html>